export const API_URL = 'https://forkify-api.jonas.io/api/v2/recipes/'; //we're using upper case because we're using constant that will never change
//it's common practice to use uppercase in config files
export const TIMEOUT_SEC = 50;
export const RES_PER_PAGE = 10;
